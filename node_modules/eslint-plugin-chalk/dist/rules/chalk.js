"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const chalk_1 = __importDefault(require("chalk"));
const chalkPlugin = {
    create: (context) => ({
        Program() {
            var _a;
            const { options } = context;
            const [option] = options;
            let message = (_a = option === null || option === void 0 ? void 0 : option.message) !== null && _a !== void 0 ? _a : 'This rule reports always.';
            if (option.icon)
                message = `${option.icon} ${message}`;
            if (option.hexColor)
                message = chalk_1.default.hex(option.hexColor)(message);
            console.log(message);
        },
    }),
};
exports.default = chalkPlugin;
